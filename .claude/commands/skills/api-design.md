Read the file agents/skills/api-design.md and execute that skill.
Follow all instructions in that file to run the API design workflow.
Use the Cortex session protocol (from CLAUDE.md) for any retrieval or memory operations.
